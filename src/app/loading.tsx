import { Wordmark } from "@/components/climbing/Wordmark";

export default function Loading() {
  return (
    <div className="mx-auto flex w-full max-w-[720px] flex-1 items-center justify-center">
      <Wordmark />
    </div>
  );
}
